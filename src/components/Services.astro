---
import { Icon } from 'astro-icon'

// interface Service {
//   services: string[];
//   icon: string[];
//   title: string[];
//   description: string[];
// }

const { services } = Astro.props;
---

<section>
    <h2 class="text-3xl text-center pb-2">Short/cut is your best way to maximise your content potential</h2>
    <p class="text-center">Let’s make your voice be heard the right way.</p>
    <ul class="flex flex-row items-top">
        {services.map((service) => (
          <li class="flex flex-col justify-center p-8 space-y-6">
            <span class="self-center">
              <Icon name={service.icon} width="72" height="72"/>
            </span>
            <h3 class="text-4xl text-center font-bold py-4">{service.title}</h3>
            <p class="text-center">{service.description}</p>
          </li>
        ))}
    </ul>
</section>

<!-- <div class="container flex">
    <ul class="flex flex-row">
        {services.map((icon,title,description) => (
          <li class="flex flex-col justify-center p-8 space-y-6">
            <span class="self-center">
            <Icon name={icon}  width="72" height="72"/>
            </span>
            <h3 class="text-4xl text-center	font-bold py-4">{title}</h3>
            <p class="text-center">{description}</p>
        </li>
        ))}
      </ul>
</div> -->